gsap.from("#mainContainer :nth-child(3)", {
  duration: 5,
  ease: "power2.out",
  x: 2000,
});

gsap.from("#mainContainer :nth-child(2)", {
  duration: 3,
  ease: "power2.out",
  x: 3000,
});

gsap.from("#mainContainer :nth-child(1)", {
  duration: 2,
  ease: "power2.out",
  x: 5000,
});
